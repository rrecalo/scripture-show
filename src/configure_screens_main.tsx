import ReactDOM from "react-dom/client";
import "./styles.css";
import ConfigureScreens from "./Components/ConfigureScreens";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
    <ConfigureScreens/>
);
