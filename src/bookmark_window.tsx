import ReactDOM from "react-dom/client";
import "./styles.css";
import BookmarkWindow from './Components/Bookmark/BookmarkWindow';

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
    <BookmarkWindow />
);
